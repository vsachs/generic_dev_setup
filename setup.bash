#!/bin/bash 

ln -s ~/git/personal/mac_setup/.bashrc ~/.bashrc
ln -s ~/git/personal/mac_setup/.emacs ~/.emacs
ln -s ~/git/personal/mac_setup/.tmux.conf ~/.tmux.conf

#brew cask install keepassxc
brew install iterm
brew install zsh
brew install wget
brew install curl
brew install tmux

#deving
brew install docker
brew install maven
